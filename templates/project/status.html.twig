{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <h3>Project status page</h3>

    <div id="project-content">
        <div class="d-grid gap-1">
            <div class="ms-3 p-1">Project: <b>{{ project.name }}</b></div>
            <div class="ms-3 p-1">Number of groups: <b>{{ project.numGroups }}</b></div>
            <div class="ms-3 p-1">Number of groups: <b>{{ project.maxStudentsPerGroup }}</b></div>
        </div>
{{ project.name }}
    </div>

    <a href="{{ path('app_project_index') }}">back to list</a>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
